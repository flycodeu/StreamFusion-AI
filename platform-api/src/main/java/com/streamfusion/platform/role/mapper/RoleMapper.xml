<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.streamfusion.platform.role.mapper.RoleMapper">
    <select id="menuIds" resultType="java.lang.Long">
        SELECT menu_id FROM sys_role_menu WHERE role_id = #{roleId} ORDER BY menu_id
    </select>
    <delete id="deleteMenus">
        DELETE FROM sys_role_menu WHERE role_id = #{roleId}
    </delete>
    <insert id="insertMenu">
        INSERT INTO sys_role_menu(role_id, menu_id, created_at, created_by)
        VALUES(#{roleId}, #{menuId}, #{now}, #{actorId})
    </insert>
    <select id="userCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM sys_user_role WHERE role_id = #{roleId}
    </select>
    <select id="userRoleIds" resultType="java.lang.Long">
        SELECT role_id FROM sys_user_role WHERE user_id = #{userId} ORDER BY role_id
    </select>
    <select id="assignedRoles" resultType="com.streamfusion.platform.role.pojo.entity.RoleEntity">
        SELECT r.id, r.code, r.name FROM sys_role r
        JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
        ORDER BY r.code
    </select>
    <delete id="deleteUserRoles">
        DELETE FROM sys_user_role WHERE user_id = #{userId}
    </delete>
    <insert id="insertUserRole">
        INSERT INTO sys_user_role(user_id, role_id, created_at, created_by)
        VALUES(#{userId}, #{roleId}, #{now}, #{actorId})
    </insert>
</mapper>
